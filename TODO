TODO

[ ] Builtins
    [x] cd
    [ ] exec
    [x] jobs
    [x] fg
    [x] bg
    [x] exit
    [ ] alias
    [ ] export
[x] Quoting (eg. ls "directory with spaces")
[ ] Comments (# Some comment)
[ ] IO Redirection (echo Something > file.txt; cat < file.txt; somecommand 2>&1 | sort)
[x] Async commands
    [x] Job control
        [x] jobs 
        [x] fg, bg (most recent job)
        [x] SIGCHLD handler to reap completed
            bg processes.
        [x] Arbitrary job fg bg

    [x] Start commands in the background (eg. somecommand & some | pipe | line &)
    [ ] Chain async comands (a & b & c)
[ ] Minimal readline-like library/code just for clara
[x] Fix zombies when running this command (for example):
    ls | grep conf | grep config | grep .h | grep .in | wc -l &
